// export const getAuth0TokenAndSendToBackend = async () => {

//     try {
//       const response = await fetch("/api/auth/token");
//       if (!response.ok) {
//         throw new Error("Error al obtener el token de Auth0");
//       }
//       const auth0TokenData = await response.json();
//       const auth0Token = auth0TokenData.token.accessToken;
//       return auth0Token;
//     } catch (error) {
//       console.error("Error al obtener o enviar el token", error);
//     }
//   };
  

